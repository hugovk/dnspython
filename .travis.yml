language: python
python:
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.6"
  #- "3.5-dev" # 3.5 development branch
  #- "3.6-dev" # 3.6 development branch
  #- "nightly" # currently points to 3.6-dev
matrix:
  allow_failures:
    - python: nightly
branches:
  except:
    - python3
install:
 - pip install pylint pycrypto ecdsa idna
script:
 - make lint
 - make test
